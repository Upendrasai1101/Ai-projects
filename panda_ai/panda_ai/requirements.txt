# Panda AI V7 — Requirements (Lightweight Build)
# Removed: torch, transformers, openai-whisper, moviepy
# These caused 500/404 errors due to HF 16GB RAM limit
# V7 uses Pollinations AI (API-based, zero local GPU)

# ── Core Flask ──
flask
flask-cors
python-dotenv

# ── HTTP + Parsing ──
requests
feedparser

# ── Search (Deep Crawl) ──
trafilatura
gnews

# ── Server ──
gunicorn

# ── File Processing ──
pdfplumber
python-docx
openpyxl
python-pptx

# ── Image OCR ──
pytesseract
Pillow

# ── Voice Input (Web Speech API handles TTS/STT in browser) ──
SpeechRecognition

# ── Timezone (IST) ──
pytz

# ── REMOVED in V7 (were causing memory issues) ──
# openai-whisper    ← 1.5GB — replaced by browser Web Speech API
# moviepy           ← heavy — video processing removed
# torch             ← 2GB   — no local ML in V7
# transformers      ← 1GB   — no local models in V7