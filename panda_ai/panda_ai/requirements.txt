# Panda AI V8.3 — Requirements (Cloud-First Build)
# Architecture: Flask backend + Groq Cloud APIs (chat, vision, whisper)
# No local ML models — everything via Groq/CF Workers AI cloud calls
# Removed legacy V7 packages no longer used: gnews, pytesseract, SpeechRecognition

# ── Core Flask ──
flask
flask-cors
python-dotenv

# ── HTTP + Parsing ──
requests
feedparser

# ── Search (Deep Crawl) ──
trafilatura

# ── Server ──
gunicorn

# ── File Processing (PDF / Word / Excel / PPTX text extraction) ──
pdfplumber
python-docx
openpyxl
python-pptx

# ── Timezone (IST) ──
pytz

# ── REMOVED in V8.3 (no longer used — replaced by cloud APIs) ──
# gnews             ← search_tool.py handles RSS directly via feedparser
# pytesseract       ← replaced by Groq Qwen Vision (qwen/qwen3.6-27b)
# Pillow            ← no local image processing needed anymore
# SpeechRecognition ← replaced by Groq Whisper (whisper-large-v3-turbo)
# openai-whisper    ← 1.5GB local model — replaced by Groq Whisper API
# moviepy           ← heavy — video handled via ffmpeg subprocess + Groq
# torch             ← 2GB   — no local ML models
# transformers      ← 1GB   — no local models